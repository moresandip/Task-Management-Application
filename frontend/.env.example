# VITE_API_URL — URL of the deployed Express backend
# For local development this is handled by vite.config.js proxy (no env var needed)
# For production, point to your Render/Railway deployment:
VITE_API_URL=https://your-backend.onrender.com/api
